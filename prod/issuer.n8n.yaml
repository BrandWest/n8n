apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: n8n-letsencrypt
  namespace: n8n
spec:
  acme:
    # The ACME server URL
    server: https://acme-v02.api.letsencrypt.org/directory # For dev env
    # Email address used for ACME registration
    email: <argocd/data/argocd-vault-plugin/cloudflare#email>
    # Name of a secret used to store the ACME account private key
    privateKeySecretRef:
      name: n8n-letsencrypt
    # ACME DNS-01 provider configurations
    solvers:
    # An empty 'selector' means that this solver matches all domains
    - selector:
        dnsNames:
        - <path:argocd/data/argocd-vault-plugin/n8n#n8n_url>
      dns01:
        cloudflare:
          email: <argocd/data/argocd-vault-plugin/cloudflare#email>
          # !! Remember to create a k8s secret before
          # kubectl create secret generic cloudflare-api-key-secret
          apiKeySecretRef:
            name: n8n-cloudflare-api-token-secret
            key: api-token
